# CAPA AMIs Inventory 2026-05-10

This report captures the current state of AMI publishing for the
Cluster API Provider for AWS, including:
- Which Kubernetes versions are tracked?
- Which AMIs are already published in the CAPA AWS account?
- Which expected AMIs are still missing?

Reports are generated automatically by the `detect-k8s-releases` workflow.

## Defaults

CAPA AMIs should be published with these configurations:

### OS

<!-- $default_os -->

### AWS Account

<!-- $default_aws_account_id -->

### Region

<!-- $default_regions -->

### Supported Kubernetes Versions

This is the list of Kubernetes versions that should have CAPA AMI support, based on the
[CAPA AMI publication policy](../../topics/images/built-amis.md#ami-publication-policy).
It is generated by the command:

`release-tool ami detect-k8s-release`

<!-- $k8s_release_table -->

## Existing AMIs List

The list of all AMIs currently published in the CAPA AWS account,
as returned by `clusterawsadm ami list`.

<!-- $ami-table -->

## Missing AMIs

AMIs that should be published for the Kubernetes versions in the Supported Kubernetes Release table above, but are not yet available.

<!-- $missing_ami_table -->

## EOL AMIs

<!-- $eol_ami_table -->
