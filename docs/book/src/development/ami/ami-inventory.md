# CAPA AMIs Inventory <ISO date>

This is the updated report that captures the current state of AMI publishing for the
Cluster API Provider for AWS, including:
- Which Kubernetes versions are tracked
- Which AMIs are already published in the CAPA AWS account
- Which expected AMIs are still missing.

Reports are generated automatically by the `detect-k8s-releases` workflow.

## Defaults

CAPA AMIs should be published with these configurations:

### OS

<!-- source: jobs.buildandpublish.strategy.matrix.target in build-and-publish-ami.yml -->

### AWS Account

<!-- source: AWS_ACCOUNT_ID env var in build-and-publish-ami.yml and build-ami-varsfile.yml -->

### Region

<!-- source: DEFAULT_REGIONS env var in build-and-publish-ami.yml and build-ami-varsfile.yml -->

### Kubernetes Release

This is the list of Kubernetes versions that have CAPA AMI support, based on the
[CAPA AMI publication policy](../../topics/images/built-amis.md#ami-publication-policy).
It is generated by the command:

`release-tool ami detect-k8s-release`

<!-- generated by release-tool ami detect-k8s-release -->

## Current Existing AMIs

<!-- AWS_ACCOUNT_ID: AWS_ACCOUNT_ID env var in build-and-publish-ami.yml and build-ami-varsfile.yml -->

The list of all AMIs currently published in AWS account `$AWS_ACCOUNT_ID`,
as returned by `clusterawsadm ami list --owner-id $AWS_ACCOUNT_ID`.

| AMI Name | Kubernetes Version | OS | Region | AMI ID | Created |
| --- | --- | --- | --- | --- | --- |

<!-- generated by: clusterawsadm ami list --owner-id $AWS_ACCOUNT_ID -->

## Missing AMIs

TBD

## EOL AMIs

TBD
